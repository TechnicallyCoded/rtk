# RTK Token-Savings TODO

## Current State

- [x] Local fork is cloned at `/Users/agent/Documents/Hyping/Agents/tools/rtk`.
- [x] Installed binary path is `/Users/agent/.cargo/bin/rtk`.
- [x] Branch for optimization work is `tcoded/opti`.
- [x] Baseline analytics commit is pushed: `Add token impact analytics`.
- [x] Priority 1 commit is pushed: `Add git show large blob guard`.
- [x] Historical stats snapshot exists locally at `history/2026-04-23.db`.
- [x] Snapshot DB is intentionally ignored by git via the existing `*.db` ignore rule.
- [x] Committed baseline includes:
  - [x] `rtk gain --impact`
  - [x] `rtk gain --impact-tree`
  - [x] `--max-depth`
  - [x] `--cmd-len`
  - [x] per-depth `--top-n-*`
  - [x] `auto-savings-rules.md`
- [x] Installed binary includes `rtk gain --db-date YYYY-MM-DD`.

## Immediate Cleanup

- [ ] Decide whether `--impact` / `--impact-tree` should be focused views that skip the normal gain summary unless another summary flag is explicitly requested.
- [x] Remove or use `avg_time_ms` in `CommandImpact`; it is now shown in the flat impact table.
- [ ] Add tests for:
  - [ ] `--impact` flag parsing.
  - [ ] `--impact-tree` flag parsing.
  - [ ] `--cmd-len` default and custom values.
  - [ ] tree cutoff label renders as `<args>`.
  - [ ] top-N defaults: depth 0 is `5`, deeper levels are `3`.

## Priority 1: Git Show Large Blob Guard

- [x] Implement a general guard for `rtk git show <ref>:<path>`.
- [x] For large, binary-ish, archive-like, compressed, minified, lockfile-like, model-like, or generated blobs, summarize instead of printing raw content.
- [x] Summary includes:
  - [x] command class: `git show blob`
  - [x] ref/path when parseable
  - [x] guessed content class
  - [x] approximate size/tokens when available
  - [x] suggested next commands
  - [x] verbose/raw escape hatch
- [x] Preserve existing behavior for normal small text blobs.
- [x] Do not special-case observed JAR names.
- [x] Raw escape hatch implemented as `rtk git show --no-compact <ref>:<path>`.
- [x] Metadata hint implemented as `rtk git ls-tree -l <ref> -- <path>`.

## Priority 2: Git Diff Large/Binary Compaction

- [ ] Improve `rtk git diff` for:
  - [ ] `--binary`
  - [ ] very large diffs
  - [ ] generated or binary patch sections
- [ ] Emit compact per-file stats, changed file list, binary patch markers, and representative hunks.
- [ ] Cap raw hunk output by file and total output.
- [ ] Provide a verbose escape hatch for full diff output.

## Priority 3: Proxy High-Output Handling

- [ ] Keep `rtk proxy` semantics conservative, but detect high-output passthrough.
- [ ] For large outputs, print a warning that proxy bypassed filtering and classify the apparent command where possible.
- [ ] Explore dispatching known safe proxied commands to existing filters only when unambiguous.
- [ ] Add explicit raw behavior if needed, such as `rtk proxy --raw`.

## Priority 4: Archive Stdout Guard

- [ ] Detect archive content streamed to stdout:
  - [ ] `tar -xOzf`
  - [ ] `unzip -p`
  - [ ] similar archive extraction commands
- [ ] Default to metadata/listing summary for large extracted content.
- [ ] Show file count, top entries by size, and safe small text samples.
- [ ] Provide a verbose/raw escape hatch.

## Priority 5: Shell Wrapper Introspection

- [ ] Parse simple `bash -lc` / `zsh -lc` wrappers only when the inner command is a single simple command or simple safe pipeline.
- [ ] Reuse existing RTK filters for recognized inner commands.
- [ ] If parsing is unsafe or complex, fall back and report that filtering was skipped.

## Analytics Follow-Ups

- [x] Add historical DB snapshot selection: `rtk gain --db-date YYYY-MM-DD`.
- [ ] Add “missed opportunity” view: highest raw/output tokens with low savings.
- [ ] Add emitted-token rank separately from saved-token rank.
- [ ] Track passthrough reason where available.
- [ ] Track parsed inner command for shell/proxy when safe.
- [ ] Track large-output classification for future prioritization.

## Validation Commands

- [x] `rtk cargo build`
- [x] `rtk rtk gain --help`
- [x] `rtk rtk gain --impact`
- [x] `rtk rtk gain --impact-tree --cmd-len 80 --max-depth 4`
- [x] `rtk rtk gain --db-date 2026-04-23 --impact-tree --cmd-len 60 --max-depth 3`
- [x] `rtk rtk gain --db-date 2026-04-23 --impact --format text`
- [x] `rtk cargo test`
- [x] Manual check: `rtk git show HEAD:Cargo.toml` prints normal text.
- [x] Manual check: `rtk git show HEAD:Cargo.lock` prints summary.
- [x] Installed updated binary to `/Users/agent/.cargo/bin/rtk`.

## Assumptions

- [x] Keep `auto-savings-rules.md` as the source of truth for optimization policy.
- [x] Do not optimize for one-off bad commands.
- [x] Prioritize general fixes ranked by raw token impact and low savings.
- [x] Keep verbose escape hatches whenever default output becomes lossy.
