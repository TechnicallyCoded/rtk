1. Add broad-search guardrails for rg
      - When rg would emit excessive matches or files, output: total matches, matching file count, top
        files by match count, and a few representative lines.
      - Include guidance to narrow path/pattern or use rg-verbose.
      - This addresses the noisy broad rg -n ... searches without hardcoding HypingChallenge paths.
  2. Add broad-file-list guardrails for rg --files
      - If rg --files emits hundreds of paths, summarize by top directories/extensions and show a small
        sample.
      - Suggest narrowing path or using rg-files-verbose.
      - This covers the repo-wide plugin listing case.
  3. Add stacktrace compaction for build tools
      - For Gradle/Maven/etc. stacktraces above a threshold, keep the failure headline, root exception,
        first relevant cause frames, and suggested rerun/diagnostic commands.
      - Preserve non-zero exit and errors prominently.
      - Add an escape hatch like gradle-verbose or passthrough mode for full stacktraces.
  4. Add large-diff summaries
      - For git diff above a threshold, default to stat/name-status plus the first few hunks or
        changed-file summary.
      - Suggest git-diff-verbose for full raw diff.
      - This is a general fix for large known diffs late in a task.
  5. Add GitLab/GitHub diff metadata preference
      - For glab mr diff / equivalent large MR diffs, summarize changed files and hunk counts first.
      - Suggest API metadata or a verbose command for raw patches.
      - Keep conflict/mergeability/status output visible.
